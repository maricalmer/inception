# Local Secrets

Create these files locally before running the project:

```sh
mysql_password.txt
mysql_root_password.txt
wp_admin_password.txt
wp_user_password.txt
```

Each file must contain only the secret value it represents.

Do not commit real secret files.
